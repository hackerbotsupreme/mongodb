// we need to connect to our aplication with the database 

const mongoose = require('mongoose')

// type mongo to start the server locally 

// mongoose provides a method to connect to the database 
mongoose.connect()